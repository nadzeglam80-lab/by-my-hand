# دليل الحلويات المنزلية – طرابلس (MVP)

منصة دليل (MVP) للبحث عن **صناع الحلويات المنزلية** و**مزوّدي مواد الخام** في طرابلس، مع فلاتر متقدمة حسب **الاسم**، **المكوّنات**، **النوع**، **السعر**، **المنطقة**، ودعم مطابقة **جميع/أي** من المكوّنات.

## 🔧 المتطلبات
- Node.js 18+
- PostgreSQL 14+

## 🚀 التشغيل محليًا
```bash
npm i
cp .env.example .env
# عدّلي DATABASE_URL إن لزم
npx prisma generate
npm run prisma:dev   # ينشئ الجداول
npm run seed         # يضيف بيانات تجريبية + يحاول تفعيل فهارس pg_trgm
npm run dev
```

افتح: http://localhost:3000

- صفحة البحث العام: `/` و`/products`
- لوحة الاستيراد (تتطلب كلمة مرور من `.env`): `/admin/login` ثم `/admin/import`

## 🔑 تسجيل الدخول للوحة المشرف
- ضعي كلمة مرور قوية في `ADMIN_PASSWORD` داخل `.env`.

## 🧪 أمثلة JSON للاستيراد
- **مواد خام**:
```json
{
  "type": "materials",
  "rows": [
    {"supplier_company":"شركة ABC", "phone":"0912345678", "whatsapp":"218912345678", "city":"طرابلس", "material_name":"سكر", "unit":"KG", "price":7.5, "min_qty":5, "availability":1, "notes":"سكر أبيض محلي"}
  ]
}
```
- **منتجات**:
```json
{
  "type": "products",
  "rows": [
    {"baker_brand":"حلويات البيت","phone":"0910000000","whatsapp":"218910000000","city":"طرابلس","area":"حي الأندلس","product_name":"بقلاوة بالفستق","category":"بقلاوة","type":"EASTERN","unit":"KG","price":60,"min_qty":0.5,"image_url":"/uploads/placeholder.png","tags":"بقلاوة;ليبية","ingredients":"فستق,سكر,زبدة"}
  ]
}
```

## 🧩 فلاتر المنتجات (API)
`GET /api/products`
- `q` – نص البحث.
- `city`, `area` – الموقع (المدينة افتراضيًا طرابلس).
- `type` – النوع: `EASTERN|LIBYAN|WESTERN|CAKE|TUNISIAN`.
- `category` – اسم الفئة.
- `ingredients` – مكوّنات مفصولة بفواصل (مثال: `فستق,لوز`).
- `match` – مطابقة المكونات: `ALL` (افتراضي) أو `ANY`.
- `unit`, `min`, `max` – تصفية السعر.

## 📦 البناء والإطلاق
- نشر سريع: Railway / Render / DigitalOcean. ارفعي المتغيرات: `DATABASE_URL`, `ADMIN_PASSWORD`, `SITE_URL`.
- تخزين الصور: يستخدم مجلّدًا محليًا `public/uploads` في الـMVP. يفضّل S3/R2 لاحقًا.

## 🗺️ خارطة الطريق
- إضافة رفع CSV/Excel فعّال.
- صفحات تفصيلية للصانع/المزوّد.
- نظام المراجعات.
- دمج Meilisearch لتحسين البحث العربي.
- دعم مدن إضافية بعد طرابلس.

## ⚠️ ملاحظات
- فهارس pg_trgm قد تتطلب صلاحيات Superuser على PostgreSQL. إن تعذّر، سيعمل البحث باستخدام `contains` بدون Fuzzy.
- هذا الإصدار يركّز على الدليل (بحث وتواصل) تمهيدًا للمنصة الكاملة.
